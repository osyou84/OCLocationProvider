import XCTest
@testable import OCLocationProvider

final class OCLocationProviderTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(OCLocationProvider().text, "Hello, World!")
    }
}
